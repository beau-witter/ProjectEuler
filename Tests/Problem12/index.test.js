import {assert} from "chai";

describe("The test", () => {
    it("passes as expected", () => {
        assert.isTrue(true);
    });

    it("fails as expected", () => {
        assert.isTrue(true);
    });
});